### Learning TypeScript with me 

- now to comprehensive the logique at framework
 * j'ai compris que en typescript il se repose essentiellement sur le JavaScript 
 * Un code javaScript est fonctionnelle sur Typescript
 * TypeScript vient juste rendre le code mieux structurel conformement a d'autres langages
 * Ont parle des Types en typeScript d'ou le nom
 * ont utilise les typage soit `type ou interface`
 * ont utilise Type # les fonctionnalite d'ou ty veux creer une fonctionnalite
 * ont utilise `interface` pour les donner exemple `User` 
 * ont peux combiner les types dans une interface largement utiliser
 * les systemes structurels ont parle `typage canard` ou  `typage structurel`